#pragma once
#include <wx/wx.h>

enum MainFrameIDs{
    DIR_PICKER = wxID_LAST + 1,
    RUN_BUTTON,
    FOLDER_LOCATION
};

enum Su {
    DirPicker = wxID_LAST + 1,
    PickerPage_Dir,
    PickerPage_SetDir
};