#pragma once
#include <wx/wx.h>

enum MainFrameIDs{
    DIR_PICKER = wxID_LAST + 1,
    RUN_BUTTON,
    FOLDER_LOCATION
};

enum FaceClusterWindowIDs {
    PREV_IMG = wxID_LAST + 1,
    NEXT_IMG,
    OPEN_IMG_LOC,
    PREV_CLUSTER,
    NEXT_CLUSTER
};